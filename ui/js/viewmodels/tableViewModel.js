// tableViewModel.js
// ViewModel para linhas da tabela analítica.

export function TableViewModel(rawRow) {
    // TODO: Normalizar dados de linha da tabela
    return {
        // Exemplo: campos normalizados, métricas, tags, etc.
    };
}
