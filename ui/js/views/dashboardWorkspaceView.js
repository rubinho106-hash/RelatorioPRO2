// dashboardWorkspaceView.js
// View do workspace para modo dashboard (KPIs, cards, analytics)

export const dashboardWorkspaceView = {
    /**
     * Renderiza o dashboard central
     * @param {Object} renderContext
     * @returns {string} HTML
     */
    render(renderContext) {
        // Exemplo: cards e KPIs (placeholder)
        return `
      <div class="dashboard-view">
        <div class="dashboard-kpis">[KPIs aqui]</div>
        <div class="dashboard-cards">[Cards e analytics aqui]</div>
      </div>
    `;
    }
};
