// detailWorkspaceView.js
// View do workspace para modo detalhe/elemento (entidade, propriedades, relações)

export const detailWorkspaceView = {
    /**
     * Renderiza o painel de detalhes
     * @param {Object} renderContext
     * @returns {string} HTML
     */
    render(renderContext) {
        // Exemplo: painel de detalhes (placeholder)
        return `
      <div class="detail-view">
        <div class="entity-panel">[Entidade e propriedades aqui]</div>
        <div class="relations-panel">[Relações e métricas aqui]</div>
      </div>
    `;
    }
};
